# RPC Direct Probe (2026-03-31)

## Goal

Re-run the 24-query evaluation against the live RPC path after the reported optimization rollout.

## What was checked

1. `search_similar_cases('반복 무단결근으로 해고된 사건', 'absence', 5)`
2. `search_similar_cases_hybrid(...)` with a real `text-embedding-3-small` query embedding
3. Local migration file currently in repo:
   - `supabase/migrations/20260331_search_similar_cases_hybrid_rpc.sql`

## Result

Both live RPC calls still returned:

```json
{"code":"57014","details":null,"hint":null,"message":"canceling statement due to statement timeout"}
```

## Interpretation

- From this environment, the live REST RPC path is still timing out.
- The local migration file in the repo also still shows the older SQL shape:
  - `compute_search_trigram_score(...)` in candidate selection
  - broad `ILIKE '%...%'`
  - no visible `is_non_labor` physical column usage
  - no visible stopword-cleaned `tq` query path

## Conclusion

The optimization described in the rollout note is not yet observable from this machine through the project REST endpoint.

That means one of the following is true:

1. the optimized SQL was deployed directly in Supabase but has not been synced back into the repo yet, or
2. the deployment did not land on the same project/function definition being called here, or
3. the function changed, but the timeout path is still being hit by REST execution.

## Next step

After the optimized function body is confirmed in the project, re-run:

- `scripts/evaluate_search_24q.py`

using direct RPC mode instead of the fallback local evaluator.
